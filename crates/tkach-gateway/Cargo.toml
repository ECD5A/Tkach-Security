[package]
name = "tkach-gateway"
version = "0.1.0"
description = "Provider-independent bounded execution boundary for Tkach Security"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tkach-core = { path = "../tkach-core" }

[lints]
workspace = true
